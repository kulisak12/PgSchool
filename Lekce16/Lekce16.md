## Listy
Java poskytuje i chytøejší listy než základní pole, a to ArrayList a LinkedList. Ty se liší jen tím, jakou datovou strukturu využívají - ArrayList je vìtšinou rychlejší.

## Mapa
Pøiøadí ke každé z hodnot další do páru. Jako implementaci lze použít HashMap. Má funkce `put` a `get`, u kterých se jako pøiøazovátko používá klíè. Fajn funkce je `keySet()`, která vrací seznam všech použitých klíèù. Pokud ke klíèi neexistuje hodnota, vrací `null`.